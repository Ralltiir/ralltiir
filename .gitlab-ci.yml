cache:
    paths:
        - node_modules/
test:
    variables:
        BROWSER: PhantomJS
    script:
        - npm install
        - npm install karma-phantomjs-launcher
        - npm run lint
        - npm run test-reports
