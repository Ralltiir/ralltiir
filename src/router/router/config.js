/**
 * @file 配置信息
 * @author treelite(c.xinle@gmail.com)
 */

define({

    /**
     * 默认的路径
     * 只对hash控制器生效
     *
     * @type {string}
     */
    path: '/',

    /**
     * 默认的根路径
     *
     * @type {string}
     */
    root: '',

    /**
     * 路由模式
     * 可选async或page
     *
     * @type {string}
     */
    mode: 'async'

});
